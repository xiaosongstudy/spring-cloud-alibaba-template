spring:
  application:
    name: nacos-consumer-one
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.164.128:8848

server:
  port: 9091

management:
  endpoints:
    web:
      exposure:
        include: "*"