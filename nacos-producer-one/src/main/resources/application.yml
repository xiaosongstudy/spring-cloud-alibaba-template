spring:
  application:
    name: nacos-provider-one
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8080
        port: 8719

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: "*"
