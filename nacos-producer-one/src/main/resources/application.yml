spring:
  application:
    name: nacos-provider-one
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.164.128:8848

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"